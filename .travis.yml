matrix:
  include:
    - language: python
      before_install: cd backend
      install:
        - pip install pipenv
        - pipenv install --dev
      script:
        - pipenv run pylint app/ -E
        - pipenv run pylint app/ --exit-zero
        - pipenv run flake8 app/
        - pipenv run black app/ --check

